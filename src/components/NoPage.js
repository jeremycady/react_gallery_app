import React from 'react';

const NoPage = () => (
  <div>
    <h3>404 - No Page Found</h3>
    <p>You have landed on a page that does not exist. Please search for another item.</p>
  </div>
);

export default NoPage;